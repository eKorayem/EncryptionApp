<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/baseline_enhanced_encryption_24"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Encryptapp"
        tools:targetApi="31">
        <activity
            android:name=".supstitution"
            android:exported="false" />
        <activity
            android:name=".VigenereCipher"
            android:exported="false" />
        <activity
            android:name=".CaeserCipher"
            android:exported="false" />
        <activity
            android:name=".main_screen"
            android:exported="false" />
        <activity
            android:name=".FullscreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:exported="false"
            android:label="@string/title_activity_fullscreen"
            android:theme="@style/Theme.Encryptapp.Fullscreen" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>